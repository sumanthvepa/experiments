/*
    Gradle build file for the basics/app module.
    This file is a modified version of the default build.gradle.kts
    file generated by the Gradle 'init' task.
 */

plugins {
  // Apply the application plugin to add support for building a CLI
  //  application in Java.
  application
}

repositories {
  // Use Maven Central for resolving dependencies.
  mavenCentral()
}

dependencies {
  // Use JUnit Jupiter for testing.
  // TODO: Why is libs.junit.jupiter an object while other dependencies are
  // strings? Research this topic
  // TODO: Why does this dependency not need a version number?
  // Research this topic
  testImplementation(libs.junit.jupiter)
  testRuntimeOnly("org.junit.platform:junit-platform-launcher")

  // This dependency is used by the application.
  // TODO: Why is libs.guava an object while other dependencies are
  // strings?
  // TODO: Why does the guava dependency nott need a version number?
  implementation(libs.guava)
  // Provides the @NotNull annotation
  implementation("org.jetbrains:annotations:24.1.0")
}

// Apply a specific Java toolchain to ease working on different environments.
java {
  toolchain {
      languageVersion = JavaLanguageVersion.of(21)
  }
}

application {
  // Define the main class for the application.
  mainClass = "sv.basics.Language"
}

tasks.named<Test>("test") {
  // Use JUnit Platform for unit tests.
  useJUnitPlatform()
}
