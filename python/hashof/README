
You have to create and activate a Python virtual environment to
properly run this code.

To create the virtual environment, install the requirements, and
activate the environment, you need to run:

python3 -m venv venv
python3 -m pip install -r requirements.txt
source venv/bin/activate


Finally execute hashof as shown below. Not the use of quotes to group
all the words in a singe sentence into one argument.

hashof 'The fitst sentence' 'The second sentence'

